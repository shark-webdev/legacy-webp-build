{
  "name": "legacy-webp-build",
  "version": "1.0.0",
  "description": "Gulp task for image compression and legacy WebP conversion.",
  "license": "MIT",
  "private": true,
  "packageManager": "pnpm@11.1.3",
  "type": "module",
  "scripts": {
    "build": "gulp",
    "compress": "gulp compressImages",
    "webp": "gulp createWebpImages",
    "clean": "gulp clean"
  },
  "devDependencies": {
    "del": "^8.0.0",
    "gulp": "^5.0.1",
    "gulp-cli": "^3.1.0",
    "sharp": "^0.34.5"
  },
  "engines": {
    "node": ">=20"
  }
}
